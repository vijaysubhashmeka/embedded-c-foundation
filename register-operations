#include<stdio.h>
#include<stdint.h>

int main()
{
    uint8_t reg = 0;

    // Step 1: Clear bits 2 and 3
    reg &= ~((1 << 2) | (1 << 3));

    // Step 2: Set value '10' into bits [3:2]
    reg |= (2 << 2);

    printf("reg = %u\n", reg);

    return 0;
}
